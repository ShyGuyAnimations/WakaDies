extends Resource

class_name TrueChar
@export var Name = "Test"
@export var pretned_i_put_the_stats : int
@export var actionList : Array 
@export var sprites = "path" 
"one sec lemme figure out what packed scene is, might be better for storing character info"
"ok so research is going somewhere probably but theres no real info about how to have an animation thingy tied to a resource, it probably works with packedscene hopefully"
"a part of me is resisting the actually helpful urge to just sit down and figure it out myself"
